# assets/

把你自己的 lofi 曲子放这里，命名为 **`bgm.mp3`**，游戏会**优先循环播放它**（左栏 🎵 打开）。

- 没有这个文件时，播放器自动回退到在线电台（SomaFM）。
- 想换曲：替换 `bgm.mp3` 即可。
- 这就是 lofi.cafe 那类站的做法：自己托管 mp3，用 HTML5 `<audio>` 播放。

> 注意版权：放你有权使用的曲子（自有 / 免版税 / CC 授权）。
